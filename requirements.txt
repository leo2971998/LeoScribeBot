py-cord[voice]>=2.6.1,<2.8
PyNaCl>=1.5.0
SpeechRecognition>=3.10.0
python-dotenv>=1.0.0
spacy>=3.7.0
thefuzz[speedup]>=0.19.0
aiofiles>=23.0.0

# Use whisper.cpp via the python binding you chose
whisper-cpp-python==0.2.0

# Build toolchain that works with the vendored whisper.cpp CMake settings
cmake<3.29
scikit-build<0.18
ninja
